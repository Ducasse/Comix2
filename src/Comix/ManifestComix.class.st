"
This test is temporary. It checks that during the transition the information duplicated between the elements and the serie are in sync.
"
Class {
	#name : #ManifestComix,
	#superclass : #PackageManifest,
	#category : #'Comix-Manifest'
}
